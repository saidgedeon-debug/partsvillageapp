import type { Part } from "@/lib/mock-data";

/**
 * Kafu / other supplier sections — cleared for clean re-import section by section.
 * O-rings live in orings-inventory.ts.
 */
export const kafuParts: Part[] = [];
